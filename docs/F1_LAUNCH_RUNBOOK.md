# Formula 1 Launch Runbook

**Built:** 26 Aug 2026 · **Spec:** two predictions per Grand Prix weekend — Qualifying Top 5 (Saturday) and Race Top 5 (Sunday), exact-position-hit scoring on the standard 5/3/2/0 values · **Provider:** Jolpica-F1 only (free; TheSportsDB cannot settle F1 — no qualifying data, no points, no standings) · **Timeline:** Monza (5–6 Sep) = hidden live rehearsal → Madrid (12–13 Sep) = stretch launch → Baku (26–27 Sep) = committed launch.

The full product/architecture assessment lives in WorkBrain:
`10-Businesses/SB/documents/SB-F1-ASSESSMENT-2026-08-26.md`.

---

## What was built (all in this repo, deployed with the next push)

**Schema (prepared, NOT applied — Dylan applies by hand, in order):**
- `supabase/migrations/073_f1_ordering.sql` — nullable prediction scores + shape trigger, `fixture_entrant_results`, `competition_standings`, `apply_ordering_scoring` (exact-hit ladder: 5 hits→exact, 3–4→gd, 1–2→result, 0→wrong on the competition's `scoring_rules` values), `settle_ordering_fixture` (atomic results+status+scoring), `rescore_fixture` routing. Score sports unchanged; IQ economy untouched by design (base points stay in the 5/3/2/0 amount_map keys; banker doubling stays aggregation-side per 055).
- `supabase/migrations/074_challenge_score_only.sql` — Matchday Challenges become explicitly score-only (four functions from 068/069 gain `prediction_type = 'score'` guards).
- `supabase/seeds/formula-1-2026.sql` — generated by `scripts/import-f1-season.mjs` from the live Jolpica schedule: competition `formula-1` (sport `motorsport`, **lifecycle draft / visible false**), 11 GP rounds (R13 Monza → R23 Abu Dhabi), 22 ordering fixtures with per-session lock times, 23 drivers as `teams` rows (constructor in `group_name`), 5/3/2/0 scoring rules, provider `jolpica`.

**Engine:** `lib/jolpica.ts` (results/qualifying/standings fetchers, constructed provider ids `f1-<season>-<round>-<q|r>`), `ingestJolpica` in the ingest cron (48-hour settle window — Jolpica is not live, so it polls started-but-unsettled sessions instead of a live window; unmapped entrants fail settlement loudly), championship standings refresh after each race.

**Client:** `lib/orderingModel.ts` (+ `tests/ordering-model.test.ts`, pinned to the SQL), `lib/f1/drivers2026.ts` (grid registry incl. Hadjar as sub cover; FIA codes — HAM/LEC/HUL/COL/STR collide with club codes on purpose, F1 never reads the club-crest map), `lib/motorsport.ts`, `SessionOrderSheet` (5-tap top-5 picker, autosave, banker, settled view with hits), `MotorsportHome`, `ChampionshipTable`, sport-kind branches in predict / matchweek / standings / stats / rules / fixture pages, F1 in the sports hub + landing + `lib/leagues/list.ts`.

**Protection (live the moment code deploys, before any DB change):**
- Venue onboarding lists **lifecycle-public competitions only** (state + leagues routes; absent settings row fails closed to draft). Not a sport filter — F1 becomes venue-selectable the moment it goes public.
- `LeagueChooser` no longer falls back to showing non-live leagues when the visibility lookup succeeds (draft F1 renders "Coming soon", never a dead link).
- The fixture detail page redirects ordering fixtures to the session sheet; `sportOf` now carries `kind: "score" | "ordering"` and every prediction surface branches on it.

**Verified:** `tsc` clean · 260/260 vitest (13 files) · `next build` clean · Jolpica endpoints verified live 26 Aug (schedule, quali, results, both standings) · seed generated from live data (22 fixtures / 11 GPs).

---

## Dylan's apply sequence (≈15 minutes, Supabase SQL editor)

1. **Apply `073_f1_ordering.sql`** (paste whole file).
2. **Apply `074_challenge_score_only.sql`**.
3. **Apply `075_prediction_write_privileges.sql`** (adversarial-audit fix — narrows client write columns so players cannot forge `points_awarded`/`is_banker`, retarget predictions, or write `updated_at`).
4. **Apply `076_rescore_admin_only.sql`** (security cleanup — `assert_admin()` gate on `rescore_fixture`/`rescore_competition` so players can't invoke admin scoring RPCs; explicit `search_path` on two legacy SECURITY DEFINER read RPCs).
5. **Apply `077_settlement_tables_readonly.sql`** (found by verify-075 on the live DB — revokes the Supabase-default client write grants on `fixtures`, `fixture_entrant_results`, `competition_standings`; no client writes them, RLS already blocked writes, this makes the grant layer agree).
6. **Run `scripts/verify-073-ordering.sql`** — every line must print ✅ (self-rolls-back; also checks post-lock ordering edits are rejected).
7. **Run `scripts/verify-075-privileges.sql`** — every line must print ✅ (settlement-controlled fields not player-writable, `updated_at` DB-managed, app paths still work).
8. **Apply the seed** `supabase/seeds/formula-1-2026.sql` (or `node scripts/run-sql.mjs --file supabase/seeds/formula-1-2026.sql`).
9. **Run `scripts/verify-phase2-isolation.sql`** — must print `✅ ISOLATION PASSED` with F1 now present (the 038 lesson: leaks only show with more competitions).
10. Deploy the code (if not already deployed).

> Migrations 075/076/077 are not F1-specific (they close holes that applied to football too), but they are safe and additive, and the hidden Monza rehearsal path does not depend on them — apply them in this sequence so the public launch is clean.
>
> Deployment notes (found while running the sequence interactively, 26 Aug 2026):
> - verify-073 §4/§5c and verify-075's identity-pin block were updated so they satisfy the production hierarchy trigger (047, every fixture needs season+round) and don't call the now-admin-gated `rescore_fixture` from the SQL editor (no `auth.uid()` there).
> - 077 exists because verify-075 caught `authenticated` holding a table-level UPDATE grant on `fixture_entrant_results` (Supabase auto-grants writes on new tables); RLS already blocked the write, 077 removes the grant too.

After step 6 the competition exists **hidden**: not in choosers, not in venue pickers, not at `/formula-1`'s public listing — but the ingest cron sees it (`ingest_enabled` is true) and will settle Monza in the background.

## Monza rehearsal (5–6 Sep) — hidden, no users

- Sat 5 Sep ~15:00 UTC (post-quali): check cron logs for `[ingest:formula-1:SETTLE] … f1-2026-13-q`; confirm `fixture_entrant_results` holds the quali classification and the fixture is `completed`.
- Sun 6 Sep ~15:30 UTC (post-race): same for `f1-2026-13-r`, plus `competition_standings` refreshed (`through_round = 13`).
- Internal test: while hidden, place staff predictions on Madrid (R14) via `/formula-1/predict` (direct URL works signed-in), then after Madrid settles confirm points land AND **IQ mints non-zero** (ledger check).
- If Jolpica lags: sessions stay unsettled and keep polling for 48h — that's designed; check `awaiting classification` lines.

## Launch flip (target: before Madrid quali, Sat 12 Sep 12:00 UTC; fallback Baku 25 Sep)

1. DB: `update competition_settings set value='"public"'::jsonb where key='lifecycle' and competition_id=(select id from competitions where slug='formula-1');` and same for `visible` → `true`. (Or `admin_launch_competition` via the wizard.)
2. Verify `/formula-1`, `/formula-1/predict`, `/sports` (F1 card replaces "Coming soon"), venue welcome picker now offers Formula 1.
3. **Venue copy** (deliberately NOT pre-shipped, so the picker never under-delivers the promise): add Formula 1 to the venue signup "everything included" checklist + welcome marketing string.
4. **Marketing OS**: F1 blocks are pre-staged in WorkBrain `content-strategy.md` (sport_rules.f1, image search, hashtags) — regenerate the SB business pack (never hand-edit the JSON), confirm `/api/marketing/fixtures` returns F1 sessions, first posts follow the no-gambling gate.
5. Announce to venues (GB funnel; the Antonelli/Russell/Hamilton title fight is the hook — 242/183/183 after Zandvoort).

## Pre-production audit (26 Aug 2026)

A full audit ran before apply: the completed Dutch GP was replayed through the
REAL migration-073 SQL in an ephemeral local Postgres (PGlite) with the REAL
production trigger bodies, and the real `lib/jolpica.ts` was exercised against
the live API. 27/27 SQL checks + 15/15 adapter checks passed, including DNF,
short-field, unknown-entrant, partial-classification and DSQ-correction cases.

**One critical finding, fixed in 073 §0 before anything was applied:** the
LIVE `enforce_prediction_deadline` short-circuits when home/away scores are
unchanged (that's how scoring updates pass). An ordering prediction's scores
are always NULL, so under the live body a user could INSERT or EDIT an F1
prediction AFTER the session started. 073 now redefines the trigger so a
payload change counts as a prediction change; the counterfactual (live body →
post-lock edit passes) and the fix (patched body → rejected) were both proven
in the replay, and `verify-073-ordering.sql` §5 re-proves it in production.

**Pre-launch hardening (26 Aug 2026), two additive items — both proven in the
same PGlite harness:**

1. **48h automated correction watch** (`app/api/cron/ingest-results/route.ts`
   `ingestJolpica`) — see the stewards'-correction procedure below. Proven:
   unchanged board = no-op; DSQ correction re-settles, replaces the
   classification without duplication, and rescores; idempotent on re-run.

2. **Adversarial write-privilege proof + fix (migration 075).** Executing as
   the real `authenticated` role under real RLS, the audit first REPRODUCED
   that a player could directly `UPDATE` their own `points_awarded` (leaderboard
   forgery, and it mints IQ at the next reconciliation) and `is_banker` (bypass
   the one-banker-per-round rule), and retarget a scored prediction to another
   fixture — because Supabase's default grants give `authenticated` full-column
   write access and RLS gates rows, not columns, while the deadline trigger's
   scores/payload short-circuit lets an unchanged-score UPDATE through. 075
   narrows the client's writable columns to exactly the app write paths and pins
   `user_id`/`fixture_id` with a trigger. Post-fix, all seven attacks are
   permission-denied while the app's own edit, `set_banker`, and the scoring
   engine still work. Not F1-specific (applied to football too) — closed now.

**Privilege sanity check (26 Aug 2026, pre-execution) — refinements to 075:**
- **`updated_at` is now database-managed.** It was client-writable but the
  deadline trigger re-stamps it to `now()` on every write and the column
  defaults to `now()`, so the client value was always overwritten. Removed
  from both the 075 grants and the two app upsert payloads
  (`lib/predictor.ts`). Proven: the trigger still stamps it with the client
  no longer sending it.
- **INSERT and UPDATE grants split explicitly.** `user_id`/`fixture_id`
  cannot be excluded from the UPDATE grant: PostgREST's upsert emits the
  on-conflict columns in `do update set` (issue #2446), so an edit-via-upsert
  needs UPDATE on them or it 403s — proven as a counterfactual in the audit.
  They stay granted; `enforce_prediction_identity` is the real immutability
  guarantee, and the audit now proves that trigger *fires* on a retarget
  attempt (not merely that the grant layer blocks it).
- **SECURITY DEFINER review:** every settlement/reconciliation function
  (`settle_ordering_fixture`, `apply_ordering_scoring`, `apply_fixture_scoring`,
  `economy_award_fixture`, `economy_reconcile`) is `REVOKE ALL` from clients;
  `set_banker` is authenticated-only; all have a safe explicit `search_path`;
  **nothing grants EXECUTE to `public`/`anon`.** Two notes (no change made,
  out of scope): `rescore_fixture`/`rescore_competition` are admin tools
  granted to `authenticated` — post-075 they can only recompute points to
  their *correct* values (no forgery, idempotent), so not a settlement risk,
  but consider gating to admin later. Two legacy read-only RPCs from migration
  001 (`get_leaderboard`, `get_all_feedback`) lack an explicit `search_path`;
  unrelated to F1/settlement — flag for a separate cleanup.

**Final security cleanup — migration 076 (26 Aug 2026, pre-execution):** both
items above were closed rather than deferred.
- `rescore_fixture`/`rescore_competition` now call `assert_admin()` — the same
  gate every other admin RPC uses. A non-admin authenticated caller gets
  "Admin privileges required."; the `/admin/fixtures` admin still rescores
  (single and nested-loop paths both proven, since `auth.uid()` persists
  through the nested SECURITY DEFINER call). EXECUTE stays with `authenticated`
  because that is the only role an admin browser session has — the body, not
  the grant, is the gate (Supabase has no separate admin Postgres role).
- `get_leaderboard` and `get_all_feedback` re-declared verbatim with
  `set search_path = public` added — identical body, identical grants, no
  behaviour change (proven: still return correct rows, `user_id` still
  excluded from the leaderboard).

## Operational procedures

**Emergency manual settlement (Jolpica down / wrong / late).** Build the
classification by hand and settle atomically — this is the same function the
cron calls, idempotent, safe to re-run:

```sql
select public.settle_ordering_fixture(
  (select id from public.fixtures where provider_fixture_id = 'f1-2026-13-r'),
  (select jsonb_agg(jsonb_build_object(
      'team_id', t.id, 'position', v.pos, 'status', v.st))
   from (values ('NOR',1,'Finished'),('ANT',2,'Finished'),('RUS',3,'Finished'),
                ('HAM',4,'Finished'),('LEC',5,'Finished')
         -- …continue to at least 15 classified rows (all starters)
        ) v(code, pos, st)
   join public.teams t on t.code = v.code
     and t.competition_id = (select id from public.competitions where slug='formula-1'))
);
```
Note the SQL requires ≥5 rows; list the full classification (retirees
included, status 'Retired') to mirror the feed. Never settle a top-5 you are
not certain of.

**Stewards' correction after settlement (DSQ, post-race penalty).** AUTOMATED
for the first 48 hours after each session (pre-launch hardening, 26 Aug 2026):
the ingest cron rechecks every settled session still inside the 48h window,
re-fetches the official classification, and if it differs from what we stored,
re-runs the idempotent `settle_ordering_fixture` — results replaced,
predictions rescored, IQ reconciled, standings refreshed. It only acts on a
genuine change (unchanged board = no-op) and never "corrects" from an
incomplete feed response (same completeness gate as first settlement). Look
for `[ingest:formula-1:CORRECTION]` in the logs. **Beyond 48 hours**,
corrections are manual: re-run `settle_ordering_fixture` with the corrected
classification (template above) — same function, still idempotent.

**Schedule changes (session moved, race postponed).** NEVER re-run the seed on
a live season — the generated SQL now refuses if predictions exist (audit
guard), because deleting fixtures cascades to predictions. Instead:

```sql
update public.fixtures set kicks_off_at = '<new UTC time>', updated_at = now()
where provider_fixture_id = 'f1-2026-17-q';
```
The rounds window recomputes automatically (042 trigger). The cron does not
sync F1 kickoff times from the feed; a session delayed to later leaves the
lock early (safe), a session moved EARLIER (essentially unheard of in F1)
needs this UPDATE promptly or predictions stay open too long. A cancelled
session: `update public.fixtures set status='postponed' …` removes it from
the settle window.

## Known v1 boundaries (deliberate)

No qualifying/sprint predictions beyond the two boards; no top-10 ordering; no live in-race data (status stays `scheduled` between session start and settlement — UI shows it locked); Matchday Challenges exclude F1 by design (074); no F1 player stats (stats tab shows a pointer to Standings); `getUserPublicPredictions` shows F1 sessions as unpredicted on public profiles (RPC returns score columns only — acceptable v1 gap); mid-season driver swaps need a registry + teams-row addition before the next session settles (the cron logs exactly this and refuses to settle a wrong board).

## 2027

Re-run `scripts/import-f1-season.mjs` when the 2027 calendar lands (announcement expected autumn 2026; provisional Bahrain opener 14 Mar 2027) — new season row, new provider ids, updated driver registry. The engine needs no changes.
